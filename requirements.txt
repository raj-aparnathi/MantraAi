# ============================================================
# Mantra AI v3.1 – Requirements  (Python 3.10 – 3.14)
# Install: pip install -r requirements.txt
#
# NOTE: PyAudioWPatch replaces PyAudio on Windows.
# ============================================================

# ── Python 3.13+ compatibility ──────────────────────────────
# audioop was removed from stdlib in 3.13; this shim restores it.
audioop-lts>=0.2.1

# ── Voice ────────────────────────────────────────────────────
SpeechRecognition>=3.17.0
pyttsx3>=2.90

# ── Neural TTS (human-sounding voice) ───────────────────────
# edge-tts: free Microsoft Edge neural voices, no API key (needs internet).
# pygame:   in-process MP3 playback of the synthesized speech.
edge-tts>=7.0.0
pygame-ce>=2.5.0

# ── Audio I/O ────────────────────────────────────────────────
# PyAudioWPatch is a maintained fork of PyAudio with Python 3.14 support.
# It provides the same 'pyaudio' module — no code changes needed.
PyAudioWPatch>=0.2.12

# ── Desktop Automation ───────────────────────────────────────
pyautogui>=0.9.54
psutil>=6.1.1
keyboard>=0.13.5
pygetwindow>=0.0.9

# ── System Volume Control (Windows) ─────────────────────────
pycaw>=20240210
comtypes>=1.4.8

# ── Screen Brightness Control ────────────────────────────────
screen-brightness-control>=0.23.0

# ── Screen Capture & Recording ───────────────────────────────
mss>=9.0.2
opencv-python>=4.10.0
Pillow>=11.0.0

# ── Clipboard ────────────────────────────────────────────────
pyperclip>=1.9.0

# ── Safe File Deletion (Recycle Bin) ─────────────────────────
send2trash>=1.8.3

# ── Internet & API calls ─────────────────────────────────────
# Used by: brain/llm_api.py (Gemini), brain/openai_llm.py (OpenAI),
#          brain/local_llm.py (Ollama), updater/updater.py (GitHub),
#          internet.py (weather/news)
requests>=2.32.3
wikipedia>=1.4.0

# ── Environment / Security ───────────────────────────────────
python-dotenv>=1.0.0

# ── RAG (Retrieval Augmented Generation) ─────────────────────
# chromadb:               Persistent vector database
# sentence-transformers:  Local embedding model (all-MiniLM-L6-v2)
#                         Lazy-loaded on first RAG query, not at startup.
# pypdf / python-docx:    Document loaders for ingestion
chromadb>=1.0.0
sentence-transformers>=2.0.0
pypdf>=4.0.0
python-docx>=1.0.0

# ── Utilities (all built-in to Python 3.10+) ─────────────────
# os, subprocess, datetime, json, threading, pathlib, shutil,
# logging, socket, time, re, ctypes, signal, zipfile, zoneinfo
# NOTE: audioop was removed in 3.13 — we use audioop-lts above.

# ── Optional: Local LLM via Ollama ───────────────────────────
# Install Ollama from https://ollama.com/download
# Then run: ollama pull llama3
# No pip package needed — Ollama is a local server.
# brain/local_llm.py connects to http://localhost:11434